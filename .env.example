# Reference for local shell environments. Keep real credentials out of this file.
# GitHub Actions uses its job-scoped token automatically.
GITHUB_TOKEN=

# Optional custom OpenAI-compatible provider for bilingual introductions.
# GitHub Actions uses GitHub Models by default, so these can remain empty.
LLM_API_KEY=
LLM_API_BASE=
LLM_MODEL=
